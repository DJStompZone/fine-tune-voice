# Training parameters
BATCH_SIZE=16
NUM_EPOCHS=20
SAVE_STEPS=500
SAVE_TOTAL_LIMIT=3
LOGGING_STEPS=100

# Path to the dataset
DATASET_PATH=/content/dataset

# Output directory for fine-tuned model and logs
OUTPUT_DIR=/content/output

# Checkpoint directory for saving intermediate training progress
CHECKPOINT_DIR=/content/checkpoints

# Discord webhook URL for notifications
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/webhook_url

# Hugging Face repository name for saving the model
HF_REPO_NAME=your-hf-repo-name

# Hugging Face authentication token
HUGGINGFACE_TOKEN=hf-123-derp
